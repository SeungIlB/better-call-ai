# Better Call AI 개발 원칙

이 문서는 이 저장소에서 작업하는 모든 에이전트가 따라야 하는 기준이다.

## 작업 진행 원칙

- 기능을 한 번에 광범위하게 구현하지 않는다.
- 작업을 작은 MVP 단계로 나누고, 각 단계가 실행·검증 가능한 상태가 된 후 다음 단계로 넘어간다.
- 사용자가 코드 작성 전에 설명을 요청하면 설계, 작업 범위, 제외 범위와 검증 기준을 먼저 설명하고 승인 전에는 코드를 변경하지 않는다.
- 현재 요청과 직접 관련 없는 리팩터링이나 미래 기능을 선제적으로 추가하지 않는다.
- 해커톤 제출에 필요한 핵심 흐름을 먼저 완성하고 Redis, GraphRAG, 파인튜닝, 조직·관리자 기능 등의 확장은 뒤로 미룬다.
- 기존 사용자 변경 사항과 관련 없는 파일은 수정하지 않는다.

## 구현 우선순위

다음 순서를 기본으로 한다. 사용자가 명시적으로 순서를 변경한 경우에는 사용자 지시를 따른다.

1. 공통 응답, 오류 코드, 예외 처리, 입력 검증, trace ID
2. JWT 기반 회원가입·로그인·재발급·로그아웃·인가
3. 개인 소유 사건의 생성·조회·수정·삭제
4. 임시 원본 파일과 확정 OCR 수정본 처리
5. 주택 임대차 생활분쟁 중심의 제한된 법률 데이터 수집
6. OpenAI API 기반 기본 RAG 검색과 출처 표시
7. 사건 분석 및 대응 안내
8. 제출 이후 Redis, 검색 고도화, GraphRAG, 파인튜닝 검토

## MVP 범위

- 서비스 대상은 개인 사용자이며 사건은 한 명의 사용자가 소유한다.
- 인가는 자기 사건과 자기 데이터에만 접근할 수 있도록 구현한다.
- 인증은 먼저 JWT와 PostgreSQL로 구현한다. Redis 세션·블랙리스트·요청 제한은 추후 확장한다.
- 증거 원본은 장기 보관하지 않는다. OCR 처리와 사용자 확인에 필요한 동안만 임시 보관하고 확정 OCR 수정본을 중심으로 저장한다.
- 최초 법률 RAG 범위는 주택 임대차의 누수, 곰팡이, 시설 하자, 수선의무, 필요비, 보증금 반환, 계약 해지로 제한한다.
- 최초 데이터는 관련 법령과 공개 판례 20~50건 수준으로 선별한다.
- 법령은 조·항·호, 판례는 판시사항·판결요지 중심으로 청킹한다.
- 법률 답변에는 법령명, 조문, 사건번호와 공식 원문 링크를 표시한다.

## Java 및 Spring 구조

- Java 21과 Spring Boot를 사용한다.
- Kotlin을 추가하지 않는다.
- 기본 요청 흐름은 `Controller → Service → ServiceImpl → Repository`로 유지한다.
- 기능 패키지 내부에서 `controller`, `service`, `serviceimpl`, `repository`, `entity`, `dto`를 사용한다.
- Controller는 HTTP 요청·응답과 검증만 담당한다.
- Service 인터페이스는 기능 계약을 정의하고 ServiceImpl이 업무 흐름과 트랜잭션을 조정한다.
- Repository는 PostgreSQL 또는 외부 API 접근을 담당한다.
- 비밀값, JWT, 개인정보, OCR 원문과 법률 상담 본문을 로그에 남기지 않는다.

## 공통 API 규칙

- 성공·실패 응답 형식을 공통 모델로 통일한다.
- 모든 응답을 HTTP 200으로 반환하지 않고 의미에 맞는 HTTP 상태 코드를 사용한다.
- 예상 가능한 업무 오류는 공통 `ErrorCode`와 `BusinessException` 계층으로 표현한다.
- 예상하지 못한 예외는 내부 상세 내용을 사용자에게 노출하지 않고 서버 로그에 trace ID와 함께 기록한다.
- 목록 API는 공통 페이지 응답 형식을 사용한다.
- API 계약이 바뀌면 `api/openapi.yaml`과 README를 함께 갱신한다.

## 테스트 및 검증

- 변경한 기능에는 정상 흐름과 핵심 실패 흐름 테스트를 작성한다.
- 커밋 전 최소한 `.\gradlew.bat test`와 `git diff --check`를 실행한다.
- 외부 API 키가 없어도 애플리케이션과 관련 없는 테스트가 실행되어야 한다.
- 외부 API 테스트는 실제 운영 API를 호출하지 않고 고정 응답 또는 로컬 테스트 서버를 사용한다.

## Git 커밋 규칙

- 커밋 메시지는 Conventional Commits 형식을 사용한다.
- 타입은 영문 소문자로 쓰고 콜론 뒤의 제목은 반드시 한글로 작성한다.
- 커밋 본문을 작성할 경우에도 반드시 한글로 작성한다.
- 하나의 커밋에는 하나의 독립적인 변경 목적만 포함한다.
- 사용자가 명시하지 않은 원격 push, force push, rebase는 수행하지 않는다.

허용 예시:

```text
refactor: 공통 API 응답 형식 정리
feat: JWT 회원가입과 로그인 구현
fix: 판례 선고일 파싱 오류 수정
test: 인증 실패 응답 테스트 추가
docs: 법률 데이터 수집 범위 정리
```

금지 예시:

```text
feat: add JWT authentication
update files
fix bug
```
